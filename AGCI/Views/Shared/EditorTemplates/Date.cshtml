
@inherits System.Web.Mvc.WebViewPage<System.DateTime?>

@if (Model.HasValue)
{
    @Html.TextBox("", String.Format("{0:mm/dd/yyyy}", Model.Value), new { @class = "form-control datepicker" })
}
else
{
    @Html.TextBox("", String.Format("{0:mm/dd/yyyy}", DateTime.Now), new { @class = "form-control datepicker" })
}

